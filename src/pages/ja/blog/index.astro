---
import Base from '../../../layouts/Base.astro';
import { getCollection } from 'astro:content';
import { t } from '../../../i18n/ui';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const posts = (await getCollection('blog'))
  .filter((p) => p.id.startsWith('ja/'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
const tagSlug = (tag: string) => tag.replace(/\s+/g, '-');
const readMin = (body: string) => Math.max(1, Math.round(body.length / 600));
---

<Base lang="ja" title="ブログ — SHINYA / 深夜">
  <div class="container">
    <section class="section">
      <h2>ブログ</h2>
      <ul class="post-list">
        {posts.map((p) => (
          <li class="card">
            <p class="post-meta">
              {p.data.date.toISOString().slice(0, 10)} · {readMin(p.body ?? '')}{t('ja', 'read.suffix')}
            </p>
            <h3 style="margin: 0.3rem 0;">
              <a href={`${base}/ja/blog/${p.id.replace(/^ja\//, '')}/`}>{p.data.title}</a>
            </h3>
            <p class="card-desc">{p.data.description}</p>
            {p.data.tags.length > 0 && (
              <ul class="tag-list">
                {p.data.tags.map((tag) => (
                  <li><a class="tag" href={`${base}/ja/blog/tag/${tagSlug(tag)}/`}>#{tag}</a></li>
                ))}
              </ul>
            )}
          </li>
        ))}
      </ul>
    </section>
  </div>
</Base>
