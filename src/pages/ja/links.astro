---
import Base from '../../layouts/Base.astro';
import ArtImg from '../../components/ArtImg.astro';
import { HYPERFOLLOW, isOut } from '../../lib/presave';

// imageに/public配下のパス(例: images/character-small.png)を入れると、
// リンクカードに小さなキャラクターサムネイルが表示されます。空ならプレースホルダー。
const streamLinks = HYPERFOLLOW.map((e) => ({
  label: `${isOut(e) ? 'ストリーミング' : 'プレセーブ'} — ${e.title}`,
  href: e.url,
  note: isOut(e)
    ? 'Spotify・Apple Musicなど各ストアで聴く'
    : `${e.dateLabel}ストリーミングリリース — プレセーブで発売と同時にライブラリへ`,
  image: `images/covers/album-${e.n}.jpg`,
}));
const officialLinks = [
  { label: 'Spotify — SHINYA 深夜', href: 'https://open.spotify.com/artist/6pNKFavzsT5AhRLq8Aj9KH', note: 'アーティストページをフォロー — すべてのリリースをひとつに', image: 'images/character-thumb.jpg' },
  { label: 'YouTube Music — SHINYA 深夜', href: 'https://music.youtube.com/@shinyatokyo', note: '公式アーティストチャンネル ♪ — アルバムと曲をまとめて聴く', image: 'images/character-thumb.jpg' },
  { label: 'YouTube — SHINYA / 深夜', href: 'https://www.youtube.com/@shinyatokyo', note: 'オリジナル深夜音楽 & ミックス', image: 'images/character-thumb.jpg' },
  { label: 'NAVERブログ (韓国語)', href: '#', note: '夜に聴く音楽の話 — 準備中', image: 'images/character-thumb.jpg' },
];
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const resolve = (p: string) => (p.startsWith('http') ? p : `${base}/${p.replace(/^\//, '')}`);
---

<Base lang="ja" title="リンク — SHINYA / 深夜">
  <div class="container">
    <section class="page-banner">
      <ArtImg class="page-banner-art" src="images/links-banner.jpg" width={1920} sizes="(min-width: 1800px) 1160px, (min-width: 1000px) 920px, 100vw" loading="eager" />
      <div class="page-banner-scrim" aria-hidden="true"></div>
      <div class="page-banner-inner">
        <h1 class="page-banner-title">深夜 LINKS</h1>
        <p class="page-banner-sub">夜のどこかでSHINYAに会う</p>
      </div>
    </section>

    <section class="section" style="max-width: 1000px; margin: 0 auto; padding-bottom: 0;">
      <h2>ストリーミング · プレセーブ</h2>
      <ul class="post-list link-grid">
        {streamLinks.map((l) => (
          <li class="card link-card">
            <a href={l.href} target="_blank" rel="noopener">
              <span class="link-thumb">
                {l.image ? (
                  <img src={resolve(l.image)} alt="" loading="lazy" />
                ) : (
                  <span class="link-thumb-kanji" aria-hidden="true">夜</span>
                )}
              </span>
              <span class="link-body">
                <strong>{l.label}</strong>
                <span class="link-note">{l.note}</span>
              </span>
              <span class="link-arrow" aria-hidden="true">→</span>
            </a>
          </li>
        ))}
      </ul>
    </section>

    <section class="section" style="max-width: 1000px; margin: 0 auto;">
      <h2>公式リンク</h2>
      <ul class="post-list link-grid">
        {officialLinks.map((l) => (
          <li class="card link-card">
            <a href={l.href} target="_blank" rel="noopener">
              <span class="link-thumb">
                {l.image ? (
                  <img src={resolve(l.image)} alt="" loading="lazy" />
                ) : (
                  <span class="link-thumb-kanji" aria-hidden="true">夜</span>
                )}
              </span>
              <span class="link-body">
                <strong>{l.label}</strong>
                <span class="link-note">{l.note}</span>
              </span>
              <span class="link-arrow" aria-hidden="true">→</span>
            </a>
          </li>
        ))}
      </ul>
      <p style="margin-top: 2rem; color: var(--text-dim); font-size: 0.85rem;">
        Apple Musicはアーティスト認証の完了後に追加されます。
      </p>
    </section>
  </div>
</Base>
